language: cpp 
sudo: false

compiler: clang

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-trusty-5.0
        packages:
            - tcsh
            - pkg-config
            - netcdf-bin
            - libnetcdf-dev
            - gfortran
            - clang-5.0

script:
    - ./icepack.setup -ts base_suite -m travisCI -testid travisCItest -e gnu && cd base_suite.travisCItest && ./results.csh

notifications:
    email: false
